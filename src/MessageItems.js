import React, { Component } from "react";
import "./MessageList.css"

class MessageItems extends Component {
       // composeMessages(message){
       //        return <li key={message.key}>{message.text}</li>
       // }
       // render() {
       //        var messageEntries = this.props.entries;
       //        var listMessages = messageEntries.map(this.composeMessages);

       //        return (
       //               <ul className="theMessage">
       //                      {listMessages}
       //               </ul>

       //        );
       // }
}

export default MessageItems;